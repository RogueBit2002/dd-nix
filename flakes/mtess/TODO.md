# TODO

- Create `uwsm` alternative for starting and stopping compositors
- Create `uwsm-app` alternative for starting applications and binding them to the compositor unit
- Fix scaling on bottom windows when at smallest size
